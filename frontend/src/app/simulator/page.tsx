import { SimulatorScreen } from "@/features/simulator/SimulatorScreen";

/** `/simulator` 요금 시뮬레이션 화면 (명세서 §10.14) — 프로필 완료 권장 */
export default function SimulatorPage() {
  return <SimulatorScreen />;
}
