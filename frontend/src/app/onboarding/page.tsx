import { OnboardingFlow } from "@/features/onboarding/OnboardingFlow";

/** `/onboarding` 온보딩 화면 (명세서 §10.2~§10.5) — 프로필 미완료 시 진입 */
export default function OnboardingPage() {
  return <OnboardingFlow />;
}
