import { AssumptionsScreen } from "@/features/settings/AssumptionsScreen";

/** `/settings/assumptions` 계산 기준 안내 (명세서 §10.15) */
export default function SettingsAssumptionsPage() {
  return <AssumptionsScreen />;
}
